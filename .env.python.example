# Python Telegram Bot 配置文件
# 重命名此文件为 .env 并填入您的实际值

# Telegram Bot Token (必需) - 从 @BotFather 获取
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Claude CLI 路径 (必需) - Windows PowerShell 脚本路径
CLAUDE_CLI_PATH=C:\Users\YourUsername\scoop\shims\claude.ps1

# 轮询设置 (可选)
POLL_INTERVAL=2
CLAUDE_TIMEOUT=60

# 日志级别 (可选: DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# 安全设置 (可选) - 留空允许所有用户
# 多个ID用逗号分隔，不要有空格
# ALLOWED_USER_IDS=123456789,987654321
# ALLOWED_CHAT_IDS=-1001234567890

# ==========================================
# 获取配置说明
# ==========================================

# 1. 创建 Telegram Bot:
#    - 向 @BotFather 发送 /newbot
#    - 按提示设置机器人名称和用户名
#    - 复制提供的 Bot Token 到上面的 TELEGRAM_BOT_TOKEN

# 2. 获取您的 User ID (用于安全限制):
#    - 向 @userinfobot 发送任意消息
#    - 机器人会回复您的用户信息

# 3. 获取群组 Chat ID (如果在群组中使用):
#    - 将机器人加入群组
#    - 在群组中发送任意消息
#    - 访问: https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
#    - 查找 "chat":{"id": 的负数值

# 4. 设置 Claude CLI 路径:
#    - Windows: 通常是 PowerShell 脚本路径
#    - 例如: C:\Users\YourName\scoop\shims\claude.ps1
#    - 或者如果在系统PATH中: claude

# 安全建议:
# - 设置 ALLOWED_USER_IDS 限制只有您可以使用机器人
# - 不要将此文件提交到公共代码仓库
# - 定期更换 Bot Token (如果怀疑泄露)