# Ultra Simple Architecture Dependencies

# Core dependencies
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.31.0
psutil>=5.9.0
aiohttp>=3.8.0  # For Discord and async HTTP operations

# Optional dependencies for different platforms
# Telegram (included by default)
# Discord (included by default)
# Add other platform dependencies as needed:
# line-bot-sdk>=3.0.0  # For Line platform
# feishu-api>=1.0.0    # For Feishu platform