# Claude Code Remote Configuration Example
# Copy this file to .env and configure with your actual values

# ===== Telegram Bot Configuration =====
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your-telegram-bot-token

# Claude CLI configuration
CLAUDE_CLI_PATH=claude
CLAUDE_WORKING_DIR=/path/to/your/project

# User authorization (optional - comma-separated list)
# If empty, all users are allowed to use the bot
ALLOWED_USER_IDS=123456789,987654321
ALLOWED_CHAT_IDS=-1001234567890

# ===== Bot Settings =====
# Polling interval in seconds
POLL_INTERVAL=2

# Claude execution timeout in seconds
CLAUDE_TIMEOUT=60

# Logging level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# ===== Proxy Configuration (Optional) =====
# HTTP proxy for Telegram API requests
HTTP_PROXY=http://127.0.0.1:7890
# Example with authentication: HTTP_PROXY=http://username:password@proxy.example.com:8080

# ===== Telegram Bot Setup Instructions =====
# 1. Create a bot:
#    - Message @BotFather on Telegram
#    - Send /newbot and follow the prompts
#    - Save the bot token you receive
#
# 2. Get your Chat ID:
#    - Message @userinfobot on Telegram
#    - Send /start to get your Chat ID
#
# 3. Configure authorization:
#    - For private use: Set ALLOWED_USER_IDS with your user ID
#    - For group use: Set ALLOWED_CHAT_IDS with your group ID
#    - For public use: Leave both empty (not recommended for security)
#
# 4. Claude CLI path:
#    - Default: 'claude' (uses system PATH)
#    - Windows: Use full path to claude.ps1 if needed
#    - Linux/macOS: Usually just 'claude' works
#
# 5. Working directory:
#    - Set to your project directory where you want Claude to work
#    - Claude will execute commands in this directory